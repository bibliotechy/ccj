<div id="content" class="<%= show_content_classes %>">
  <p id="notice"><%= notice %></p>
  <h2>
    <%= @work.title %>
  </h2>

  <p> <strong> Artists </strong>
    <ul>
      <%= @work.artists.uniq.collect do |artist| %>
        <%= content_tag(:li, artist.name) %>
      <% end.join.html_safe %>
    </ul>
  </p>


    <p>
      <strong>Description</strong>
    </p>
    <p>
      <%= @work.description_en %>
      <br />
      <br />
      <%= @work.description_jp %>
    </p>

<h3>Components</h3>
  <p>
      <%= @work.components.collect do |component| %>
      <div>
        <%= render('components/fields', component: component) %>
      </div>

      <% end.join.html_safe %>
    </ul>
  </p>


  <p>
    <strong>Local:</strong>
    <%= @work.local_id %>
  </p>

</div>
