<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_en %>
    <%= form.text_area :name_en, id: :artist_name_en %>
  </div>

  <div class="field">
    <%= form.label :name_jp %>
    <%= form.text_area :name_jp, id: :artist_name_jp %>
  </div>

  <div class="field">
    <%= form.label :viaf_identifier %>
    <%= form.text_field :viaf_identifier, id: :artist_viaf_identifier %>
  </div>

  <div class="field">
    <%= form.label :local_identifier %>
    <%= form.text_field :local_identifier, id: :artist_local_identifier %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
